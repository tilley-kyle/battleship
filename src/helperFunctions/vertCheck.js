
const vertCheck =(board, ship) => {
  const shipSizes = {
    carrier: 5,
    battleship: 4,
    destroyer: 3,
    submarine: 3,
    patrolboat: 2,
  }
  const length = shipSizes[ship];
  
};

export default vertCheck;